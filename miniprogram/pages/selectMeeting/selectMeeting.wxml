<view class="meetingWrap" >
    <view class="meeting" bindtap="toTitleMeeting" wx:for="{{selectTypeSession}}" wx:key="index" bindtap="toSelectMore" data-info="{{item.id}}">{{item.title}}</view>
    <!--<view class="meeting" bindtap="toAreaMeeting">区域招聘会</view>-->
</view>
